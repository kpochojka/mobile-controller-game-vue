# Base URL encoded in the QR code (phone must reach your Mac on LAN). Update if your IP changes.
VITE_CONTROLLER_ORIGIN=http://192.168.0.212:5173
# Express + Socket.io (relay). Use the port printed when you run node server.js
VITE_SOCKET_IO_TARGET=http://localhost:3002
