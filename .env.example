# Dev: URL your phone opens when scanning the game QR (same Wi‑Fi as PC). Omit to use browser origin (localhost won’t work on phone).
# VITE_CONTROLLER_ORIGIN=http://192.168.0.212:5173

# Dev: Express + Socket.io server for Vite proxy (if not on default :3000).
# VITE_SOCKET_IO_TARGET=http://localhost:3000
