# Dev: optional LAN origin for QR when using Vite (path /jungle-controller is appended).
# VITE_CONTROLLER_ORIGIN=http://192.168.0.212:5173

# Dev: Express + Socket.io URL for Vite /socket.io proxy.
# VITE_SOCKET_IO_TARGET=http://localhost:3000
